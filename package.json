{
  "name": "co-ca-ngua",
  "version": "1.0.0",
  "description": "Ludo game 2-10 players",
  "scripts": {
    "start": "node server/index.js",
    "build": "cd server && npm install && cd ../client && npm install --include=dev && npm run build"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
